---
- name: Refresh apt package cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install CKA node packages
  ansible.builtin.apt:
    name: "{{ cka_node_packages }}"
    state: present
