# Managed by Ansible (samba_fileserver role)
# WS-Discovery daemon for Windows network browsing (wsdd-server.service).

WSDD_PARAMS="--workgroup {{ fileserver_wsdd_workgroup }} --hostname {{ fileserver_wsdd_host }}"
