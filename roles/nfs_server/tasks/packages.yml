---
- name: NFS server | Refresh apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: NFS server | Install NFS server packages
  ansible.builtin.apt:
    name: "{{ nfs_server_packages }}"
    state: present
