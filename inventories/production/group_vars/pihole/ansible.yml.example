---
# Pi-hole SSH — copy to ansible.yml only for pre-repave CentOS/Rocky (root-only).
#
#   cp inventories/production/group_vars/pihole/ansible.yml.example \
#      inventories/production/group_vars/pihole/ansible.yml
#
# After Ubuntu repave + domain-join, delete ansible.yml — cloud-init ansible user is default.

# ansible_user: root
