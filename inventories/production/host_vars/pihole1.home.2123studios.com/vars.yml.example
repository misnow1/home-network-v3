---
# pihole-1 (kif) — copy to vars.yml (gitignored alongside production inventory).
#
# Stagger reboot window after pihole-2. Local time: America/New_York.

unattended_upgrades_auto_reboot: true
unattended_upgrades_reboot_time: "03:45"
